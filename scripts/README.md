# Scripts

## build.sh

This script builds the firmware of the Quantum Dice.

To run the script, use the following command in your terminal:

```bash
cd path/to/your/repo
sh scripts/build.sh
```
